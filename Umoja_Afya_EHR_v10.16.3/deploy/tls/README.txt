Place the production TLS certificate files here before deployment:

- fullchain.pem
- privkey.pem

Files must be readable only by the deployment administrator and the Docker engine.
Use certificates issued for UMOJA_PUBLIC_HOST. Do not use self-signed certificates for live clinical service.

# Runtime data directory

No database, user account, password hash, patient record or credential is shipped in this production archive.

The Docker review profile creates a fresh PostgreSQL volume and loads synthetic clinical data only. The first administrator is created through the one-time token-protected setup screen.
